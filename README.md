# ProcurePilot AI

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Demo files

Use these two files:
- `data/sample_vendor_request.txt`
- `data/sample_vendor_quotes.csv`

The app also supports built-in sample mode.
